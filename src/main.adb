with Disciplina_Crud; use Disciplina_Crud;
--with Ada.Text_IO.Integer_IO;
with Ada.Text_IO; use Ada.Text_IO;

procedure Main is
   D : Disciplina;
begin
 --  D := Incrementa(D);
   --Put(D.Codigo);
--   Ada.Text_IO.Integer_IO.Put(D.Codigo);
 --   Disciplina_Crud.Incrementa;
 Ada.Text_IO.Put_Line("opa");
end Main;
